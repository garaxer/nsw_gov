{
  "name": "gary-address",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:stack": "cd infrastructure/ && npm run build",
    "deploy": "cd infrastructure/ && npm run deploy",
    "destroy": "cd infrastructure/ && npm run destroy",
    "dev": "npm run dev --workspace=handlers",
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "infrastructure",
    "handlers"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}